# Next의 output:'standalone' 파일 트레이싱이 pnpm의 심링크 node_modules를 제대로 따라가지
# 못해, 컨테이너에서 @swc/helpers를 찾지 못하고 기동 즉시 죽는다(MODULE_NOT_FOUND).
# npm과 같은 평평한 레이아웃으로 깔아 트레이싱이 실제 경로를 보게 한다.
# 디스크 절약은 포기하지만 lockfile·설치 속도는 그대로다.
node-linker=hoisted
